targets:
  $default:
    builders:
      build_web_compilers|entrypoint:
        generate_for:
          include:
            - "test/**_test.dart"
            - "example/main.dart"
        release_options:
          dart2js_args:
            - --no-minify